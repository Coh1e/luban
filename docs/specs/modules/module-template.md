# 模块文档模板

> 拷贝本文件到 `modules/<module-name>.md` 并填空。每个模块一个文件，便于
> blame 和精细化 PR review。短小是优点；超过 300 行需要考虑是否拆分。

## <module-name>

**源文件**：`src/<path>.cpp`、`src/<path>.hpp`
**职责**：一句话描述本模块对外承诺什么。
**v1 状态**：稳定 / 演进中 / 实验性。

## 公共 API

| 函数 / 类型 | 签名 | 说明 |
|---|---|---|
| `foo()` | `int foo(const Bar&)` | 一行说明 |

## 不变量

- 列出本模块**对外**承诺的不变量（被其他模块依赖的那些性质）

## 内部结构

可选：私有函数、数据结构、状态机。

## 与其他模块的关系

- 调用：`<module>` ← 我调它
- 被调用：`<module>` → 它调我

## 已知限制 / TODO

- ...

## 测试覆盖

- 单测：当前 0%（M3+ 接入 doctest）
- 手测路径：哪几个 verb 会触发本模块
